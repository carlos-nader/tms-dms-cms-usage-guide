name: Generate Integrated Files Report

on:
  push:
    paths:
      - 'wip/**/*.tex'
      - 'archive/**/*.tex'
      - 'docs/**/*.md'
      - 'guide.tex'
      - 'misc/STYLE-GUIDE.md'
  workflow_dispatch:

permissions:
  contents: write
  issues: read
  pull-requests: read

jobs:
  generate-report:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository with SSH
        uses: actions/checkout@v4
        with:
          ssh-key: ${{ secrets.DEPLOY_KEY }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run report generator
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          python3 scripts/generate-integrated-files.py 2>&1

      - name: 📄 Convert Markdown to HTML
        uses: natescherer/markdown-to-html-with-github-style-action@v1.1.0
        with:
          path: INTEGRATED-FILES.md
          outputpath: docs

      - name: ✏️ Rename HTML file
        run: |
          mv docs/INTEGRATED-FILES.html docs/integrated-files.html
      
      - name: ⚙️ Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: 💾 Commit and push changes
        run: |
          echo "=== Checking for changes ==="
          git status
          git add INTEGRATED-FILES.md INTEGRATED-FILES.json docs/integrated-files.html
          if ! git diff --cached --quiet; then
            echo "✓ Changes detected, committing..."
            git commit -m "chore: update integrated files report [skip ci]"
            echo "✓ Pushing to main branch..."
            git pull --rebase origin main
            git push origin main
            echo "✅ Push successful!"
          else
            echo "ℹ️ No changes to commit"
          fi
